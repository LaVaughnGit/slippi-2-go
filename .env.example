# Copy this to .env and fill in your values
DISCORD_TOKEN=your_bot_token_here
GUILD_ID=1509217627908735106   # right-click your server → Copy Server ID (enable Dev Mode first)
DB_PATH=slippi_bot.db          # SQLite database file path
SYNC_INTERVAL_MINUTES=45       # how often to refresh all ranks (be polite to Slippi's servers)
